export type Associate = {
  idAssociate: string;
  firstName: string;
  lastName: string;
  cpf: string;
  active: boolean;
  createdAt: string;
  updatedAt: string;
};

export type AssociateStats = {
  totalAssociates: number;
};
